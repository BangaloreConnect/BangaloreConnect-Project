<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- BASIC SEO -->
    <title>Bangalore Connect - Find IT & Non-IT Jobs in Bangalore | Job Portal</title>
    <meta name="description" content="Find latest IT jobs, Software Engineer positions, Non-IT careers in Bangalore. Job listings with salary details and direct apply links.">
    <meta name="keywords" content="Bangalore jobs, IT jobs Bangalore, software developer jobs, fresher jobs Bangalore, experienced jobs">
    <meta name="author" content="Bangalore Connect">
    
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap">
    <style>
        /* COMPACT Job Card Styles */
        .compact-job-card {
            background: white;
            border-radius: 14px;
            padding: 20px;
            transition: all 0.25s ease;
            position: relative;
            border: 1px solid #e2e8f0;
            overflow: hidden;
            margin-bottom: 16px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
            max-width: 100%;
        }

        .compact-job-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            border-color: #3b82f6;
        }

        .compact-job-card.featured {
            border-left: 5px solid #f59e0b;
            background: linear-gradient(135deg, #fff7ed 0%, #fff 100%);
        }

        .compact-job-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 16px;
            gap: 16px;
        }

        .compact-job-title-section {
            flex: 1;
        }

        .compact-job-title {
            font-size: 18px;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 6px;
            line-height: 1.3;
        }

        .compact-job-company {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #64748b;
            font-size: 14px;
            font-weight: 500;
        }

        .compact-job-company i {
            color: #94a3b8;
            font-size: 13px;
        }

        .compact-featured-badge {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            color: white;
            padding: 5px 12px;
            border-radius: 18px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            box-shadow: 0 2px 6px rgba(245, 158, 11, 0.2);
        }

        .compact-details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 12px;
            margin-bottom: 16px;
            padding: 16px;
            background: #f8fafc;
            border-radius: 10px;
            border: 1px solid #e2e8f0;
        }

        .compact-detail-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 10px;
            background: white;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
            min-height: 48px;
        }

        .compact-detail-icon {
            width: 32px;
            height: 32px;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 13px;
            flex-shrink: 0;
        }

        .compact-detail-content {
            flex: 1;
            min-width: 0;
        }

        .compact-detail-label {
            font-size: 11px;
            color: #64748b;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            margin-bottom: 1px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .compact-detail-value {
            font-size: 14px;
            font-weight: 600;
            color: #1e293b;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .compact-salary .compact-detail-icon {
            background: #d1fae5;
            color: #065f46;
        }

        .compact-type .compact-detail-icon {
            background: #dbeafe;
            color: #1e40af;
        }

        .compact-experience .compact-detail-icon {
            background: #fef3c7;
            color: #92400e;
        }

        .compact-location .compact-detail-icon {
            background: #fce7f3;
            color: #9d174d;
        }

        .compact-description {
            color: #475569;
            font-size: 13px;
            line-height: 1.5;
            margin-bottom: 18px;
            padding: 14px;
            background: #f8fafc;
            border-radius: 6px;
            border-left: 3px solid #3b82f6;
            max-height: 60px;
            overflow: hidden;
            position: relative;
        }

        .compact-description::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 20px;
            background: linear-gradient(to bottom, transparent, #f8fafc);
        }

        .compact-job-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 16px;
            border-top: 1px solid #e2e8f0;
        }

        .compact-time-section {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .compact-posted-time {
            display: flex;
            align-items: center;
            gap: 6px;
            color: #64748b;
            font-size: 12px;
            font-weight: 500;
        }

        .compact-posted-time i {
            color: #3b82f6;
            font-size: 11px;
        }

        .compact-share-btn {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 8px 16px;
            background: linear-gradient(135deg, #25D366 0%, #128C7E 100%);
            color: white;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            white-space: nowrap;
        }

        .compact-share-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
        }

        .compact-action-buttons {
            display: flex;
            gap: 10px;
        }

        .compact-view-btn {
            padding: 8px 18px;
            background: #3b82f6;
            color: white;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            white-space: nowrap;
        }

        .compact-view-btn:hover {
            background: #2563eb;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }

        .compact-apply-btn {
            padding: 8px 18px;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            white-space: nowrap;
        }

        .compact-apply-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }

        /* Smaller hero section */
        .compact-hero-section {
            padding: 2.5rem 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin-bottom: 1.5rem;
            border-radius: 0 0 16px 16px;
        }

        .compact-hero-content {
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .compact-hero-title {
            font-size: 2.2rem;
            font-weight: 800;
            color: white;
            margin-bottom: 0.75rem;
            line-height: 1.2;
        }

        .compact-hero-subtitle {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 2rem;
        }

        /* Smaller main wrapper */
        .compact-main-wrapper {
            display: flex;
            gap: 24px;
            padding: 1.5rem 0;
            flex: 1;
        }

        /* Smaller sidebar */
        .compact-sidebar {
            width: 280px;
            flex-shrink: 0;
        }

        .compact-sidebar-section {
            background: white;
            border-radius: 14px;
            padding: 1.25rem;
            margin-bottom: 1.25rem;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
            border: 1px solid #e2e8f0;
        }

        /* Responsive adjustments */
        @media (max-width: 1024px) {
            .compact-main-wrapper {
                flex-direction: column;
            }
            
            .compact-sidebar {
                width: 100%;
            }
            
            .compact-hero-title {
                font-size: 1.8rem;
            }
        }

        @media (max-width: 768px) {
            .compact-job-header {
                flex-direction: column;
                gap: 12px;
            }
            
            .compact-details-grid {
                grid-template-columns: 1fr;
            }
            
            .compact-job-footer {
                flex-direction: column;
                gap: 12px;
                align-items: stretch;
            }
            
            .compact-time-section {
                justify-content: space-between;
            }
            
            .compact-action-buttons {
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            .compact-job-card {
                padding: 16px;
            }
            
            .compact-hero-title {
                font-size: 1.5rem;
            }
            
            .compact-hero-subtitle {
                font-size: 1rem;
            }
            
            .compact-share-btn,
            .compact-view-btn,
            .compact-apply-btn {
                padding: 7px 14px;
                font-size: 12px;
            }
            
            .compact-detail-item {
                padding: 5px 8px;
            }
        }
    </style>
</head>
<body>
    <!-- Modern Header with Admin Button Only on Homepage -->
    <header class="site-header">
        <div class="container">
            <div class="header-content">
                <a href="/" class="logo">
                    <div class="logo-icon">
                        <i class="fas fa-briefcase"></i>
                    </div>
                    <div class="logo-text">
                        <h1>Bangalore Connect</h1>
                        <span>Your Gateway to Tech Careers</span>
                    </div>
                </a>
                <div class="header-actions">
                    <a href="https://chat.whatsapp.com/KhTXl9CNMbSG8mv6nYCAAW" target="_blank" class="btn btn-outline">
                        <i class="fab fa-whatsapp"></i> Join Community
                    </a>
                    <% if (showAdminButton) { %>
                        <a href="/admin" class="btn btn-warning">
                            <i class="fas fa-user-shield"></i> Admin Login
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </header>

    <!-- Compact Hero Section -->
    <section class="compact-hero-section">
        <div class="container">
            <div class="compact-hero-content">
                <h1 class="compact-hero-title">Find Your Dream Job in Bangalore</h1>
                <p class="compact-hero-subtitle">Discover thousands of job opportunities from top companies</p>
                
                <div class="search-container">
                    <form action="/" method="GET">
                        <div class="search-box">
                            <input type="text" name="search" class="search-input" 
                                   placeholder="Search jobs by role, company, or keyword..." 
                                   value="<%= query.search || '' %>">
                            <button type="submit" class="search-btn">
                                <i class="fas fa-search"></i> Search Jobs
                            </button>
                        </div>
                        <input type="hidden" name="type" value="<%= query.type || 'all' %>">
                        <input type="hidden" name="experience" value="<%= query.experience || 'all' %>">
                        <input type="hidden" name="location" value="<%= query.location || 'all' %>">
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <div class="container">
        <div class="compact-main-wrapper">
            <!-- Compact Sidebar -->
            <aside class="compact-sidebar">
                <!-- Filters -->
                <div class="compact-sidebar-section">
                    <div class="section-title">
                        <i class="fas fa-filter"></i> Filter Jobs
                    </div>
                    
                    <form action="/" method="GET">
                        <input type="hidden" name="search" value="<%= query.search || '' %>">
                        
                        <div class="filter-group">
                            <label class="filter-label">Job Type</label>
                            <select name="type" class="filter-select">
                                <option value="all">All Job Types</option>
                                <% jobTypes.forEach(type => { %>
                                    <option value="<%= type %>" 
                                            <%= query.type === type ? 'selected' : '' %>>
                                        <%= type %> Jobs
                                    </option>
                                <% }); %>
                            </select>
                        </div>
                        
                        <div class="filter-group">
                            <label class="filter-label">Experience Level</label>
                            <select name="experience" class="filter-select">
                                <option value="all">All Experience Levels</option>
                                <% experiences.forEach(exp => { %>
                                    <option value="<%= exp %>" 
                                            <%= query.experience === exp ? 'selected' : '' %>>
                                        <%= exp %>
                                    </option>
                                <% }); %>
                            </select>
                        </div>
                        
                        <div class="filter-group">
                            <label class="filter-label">Location</label>
                            <select name="location" class="filter-select">
                                <option value="all">All Locations</option>
                                <% locations.forEach(loc => { %>
                                    <option value="<%= loc %>" 
                                            <%= query.location === loc ? 'selected' : '' %>>
                                        <%= loc %>
                                    </option>
                                <% }); %>
                            </select>
                        </div>
                        
                        <button type="submit" class="filter-btn">
                            <i class="fas fa-check-circle"></i> Apply Filters
                        </button>
                       
                    </form>
                </div>

                <!-- Career Resources -->
                <div class="compact-sidebar-section">
                    <div class="section-title">
                        <i class="fas fa-rocket"></i> Resources
                    </div>
                    <nav class="sidebar-nav">
    <a href="/resume-builder" class="nav-link">
        <i class="fas fa-file-alt"></i> Resume Builder
    </a>
    <a href="/interview-prep" class="nav-link">
        <i class="fas fa-comments"></i> Interview Preparation
    </a>
    <a href="/companies" class="nav-link">
        <i class="fas fa-building"></i> Top Companies
    </a>
    <a href="/career-guide" class="nav-link">
        <i class="fas fa-chart-line"></i> Career Growth Guide
    </a>
    <a href="/ai-resources" class="nav-link">
        <i class="fas fa-robot"></i> AI Career Tools
    </a>
    <a href="/multilingual" class="nav-link">
        <i class="fas fa-language"></i> Multilingual Support
    </a>
    <a href="/blog" class="nav-link">
        <i class="fas fa-blog"></i> Tech Blog
    </a>
</nav>
                </div>

                <!-- Quick Stats -->
                <div class="compact-sidebar-section">
                    <div class="section-title">
                        <i class="fas fa-chart-bar"></i> Job Stats
                    </div>
                    <div style="display: grid; gap: 10px;">
                        <div style="display: flex; justify-content: space-between; align-items: center;">
                            <span style="color: var(--gray); font-size: 13px;">Total Jobs:</span>
                            <span style="color: var(--primary); font-weight: 700; font-size: 15px;"><%= totalJobs %></span>
                        </div>
                        <div style="display: flex; justify-content: space-between; align-items: center;">
                            <span style="color: var(--gray); font-size: 13px;">IT Jobs:</span>
                            <span style="color: var(--primary); font-weight: 700; font-size: 15px;"><%= jobTypes.includes('IT') ? jobs.filter(j => j.type === 'IT').length : 0 %></span>
                        </div>
                        <div style="display: flex; justify-content: space-between; align-items: center;">
                            <span style="color: var(--gray); font-size: 13px;">Featured:</span>
                            <span style="color: var(--warning); font-weight: 700; font-size: 15px;"><%= jobs.filter(j => j.featured).length %></span>
                        </div>
                    </div>
                </div>
            </aside>
            
            <!-- Main Content Area -->
            <main class="main-content">
                <!-- Content Header -->
                <div class="content-header">
                    <h2 style="font-size: 1.6rem;">Latest Job Opportunities</h2>
                    <div class="job-count" style="padding: 8px 16px; font-size: 14px;">
                        <i class="fas fa-briefcase"></i> <%= totalJobs %> Jobs
                    </div>
                </div>
                
                <!-- Compact Jobs List -->
                <% if (jobs.length === 0) { %>
                    <div style="text-align: center; padding: 3rem 1.5rem; background: white; border-radius: 14px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); margin: 1rem 0;">
                        <i class="fas fa-search" style="font-size: 48px; color: var(--gray-light); margin-bottom: 1rem;"></i>
                        <h3 style="color: var(--gray-dark); margin-bottom: 0.5rem; font-size: 1.25rem;">No jobs found matching your criteria</h3>
                        <p style="color: var(--gray); max-width: 350px; margin: 0 auto 1.5rem; font-size: 14px;">Try adjusting your filters or search terms.</p>
                        <a href="/" class="btn btn-primary" style="padding: 10px 24px; font-size: 14px;">
                            <i class="fas fa-redo"></i> Reset Search
                        </a>
                    </div>
                <% } else { %>
                    <div style="display: flex; flex-direction: column; gap: 16px;">
                        <% jobs.forEach(job => { %>
                            <div class="compact-job-card <%= job.featured ? 'featured' : '' %>">
                                <!-- Job Header -->
                                <div class="compact-job-header">
                                    <div class="compact-job-title-section">
                                        <h3 class="compact-job-title"><%= job.role %></h3>
                                        <div class="compact-job-company">
                                            <i class="fas fa-building"></i>
                                            <span><%= job.company %></span>
                                        </div>
                                    </div>
                                    <% if (job.featured) { %>
                                        <span class="compact-featured-badge">
                                            <i class="fas fa-star"></i> Featured
                                        </span>
                                    <% } %>
                                </div>
                                
                                <!-- Job Details Grid -->
                                <div class="compact-details-grid">
                                    <div class="compact-detail-item compact-salary">
                                        <div class="compact-detail-icon">
                                            <i class="fas fa-rupee-sign"></i>
                                        </div>
                                        <div class="compact-detail-content">
                                            <div class="compact-detail-label">Salary</div>
                                            <div class="compact-detail-value">₹<%= job.salary %>/month</div>
                                        </div>
                                    </div>
                                    
                                    <div class="compact-detail-item compact-type">
                                        <div class="compact-detail-icon">
                                            <i class="fas fa-briefcase"></i>
                                        </div>
                                        <div class="compact-detail-content">
                                            <div class="compact-detail-label">Job Type</div>
                                            <div class="compact-detail-value"><%= job.type %></div>
                                        </div>
                                    </div>
                                    
                                    <div class="compact-detail-item compact-experience">
                                        <div class="compact-detail-icon">
                                            <i class="fas fa-user-tie"></i>
                                        </div>
                                        <div class="compact-detail-content">
                                            <div class="compact-detail-label">Experience</div>
                                            <div class="compact-detail-value"><%= job.experience %></div>
                                        </div>
                                    </div>
                                    
                                    <div class="compact-detail-item compact-location">
                                        <div class="compact-detail-icon">
                                            <i class="fas fa-map-marker-alt"></i>
                                        </div>
                                        <div class="compact-detail-content">
                                            <div class="compact-detail-label">Location</div>
                                            <div class="compact-detail-value"><%= job.location %></div>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Job Description -->
                                <div class="compact-description">
                                    <%- job.description.substring(0, 180) + (job.description.length > 180 ? '...' : '') %>
                                </div>
                                
                                <!-- Combined Footer -->
                                <div class="compact-job-footer">
                                    <div class="compact-time-section">
                                        
                                        <div class="compact-posted-time">
                                            <i class="far fa-clock"></i>
                                            <span>Posted: <%= job.postedDate || 'Recently' %></span>
                                        </div>
                                        <a href="https://wa.me/?text=Check this job: <%= encodeURIComponent(job.role) %> at <%= encodeURIComponent(job.company) %> - Salary: ₹<%= job.salary %>/month - Location: <%= job.location %> - Apply Now: <%= encodeURIComponent(job.applyLink) %>%0A%0AJoin our WhatsApp community: https://chat.whatsapp.com/KhTXl9CNMbSG8mv6nYCAAW" 
                                           target="_blank" class="compact-share-btn">
                                            <i class="fab fa-whatsapp"></i>
                                            Share
                                        </a>
                                    </div>
                                    
                                    <div class="compact-action-buttons">
                                        <a href="/job/<%= job.id %>" class="compact-view-btn">
                                            <i class="fas fa-eye"></i>
                                            View Details
                                        </a>
                                        <a href="<%= job.applyLink %>" target="_blank" class="compact-apply-btn">
                                            <i class="fas fa-paper-plane"></i>
                                            Apply Now
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    
                    <!-- Pagination -->
                    <% if (totalPages > 1) { %>
                        <div class="pagination">
                            <% if (page > 1) { %>
                                <a href="/?page=<%= page - 1 %>&search=<%= query.search || '' %>&type=<%= query.type || 'all' %>&experience=<%= query.experience || 'all' %>&location=<%= query.location || 'all' %>" 
                                   class="page-btn">
                                    <i class="fas fa-chevron-left"></i>
                                </a>
                            <% } %>
                            
                            <% for(let i = 1; i <= totalPages; i++) { %>
                                <% if (i === page) { %>
                                    <a href="/?page=<%= i %>&search=<%= query.search || '' %>&type=<%= query.type || 'all' %>&experience=<%= query.experience || 'all' %>&location=<%= query.location || 'all' %>" 
                                       class="page-btn active">
                                        <%= i %>
                                    </a>
                                <% } else if (i === 1 || i === totalPages || (i >= page - 2 && i <= page + 2)) { %>
                                    <a href="/?page=<%= i %>&search=<%= query.search || '' %>&type=<%= query.type || 'all' %>&experience=<%= query.experience || 'all' %>&location=<%= query.location || 'all' %>" 
                                       class="page-btn">
                                        <%= i %>
                                    </a>
                                <% } %>
                            <% } %>
                            
                            <% if (page < totalPages) { %>
                                <a href="/?page=<%= page + 1 %>&search=<%= query.search || '' %>&type=<%= query.type || 'all' %>&experience=<%= query.experience || 'all' %>&location=<%= query.location || 'all' %>" 
                                   class="page-btn">
                                    <i class="fas fa-chevron-right"></i>
                                </a>
                            <% } %>
                        </div>
                    <% } %>
                <% } %>
            </main>
        </div>
    </div>

    <!-- Compact Footer -->
    <footer class="site-footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Bangalore Connect</h3>
                    <p style="margin-bottom: 1rem; color: var(--gray-light); font-size: 13px;">We connect the best talent with the top companies across the city to help you land your dream job faster</p>
                    <div style="display: flex; gap: 10px;">
                        <a href="https://chat.whatsapp.com/KhTXl9CNMbSG8mv6nYCAAW" target="_blank" class="action-btn whatsapp-btn" style="width: 34px; height: 34px; font-size: 14px;">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                        <a href="#" class="action-btn share-btn" style="width: 34px; height: 34px; font-size: 14px;">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a href="#" class="action-btn copy-btn" style="width: 34px; height: 34px; font-size: 14px;">
                            <i class="fab fa-twitter"></i>
                        </a>
                    </div>
                </div>
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <div class="footer-links">
                        <a href="/"><i class="fas fa-home"></i> Home</a>
                        <a href="/resume-builder"><i class="fas fa-file-alt"></i> Resume Builder</a>
                        <a href="/interview-prep"><i class="fas fa-comments"></i> Interview Prep</a>
                        <a href="/companies"><i class="fas fa-building"></i> Companies</a>
                    </div>
                </div>
                <div class="footer-section">
                    <h3>Resources</h3>
                    <div class="footer-links">
                        <a href="/career-guide"><i class="fas fa-chart-line"></i> Career Guide</a>
                        <a href="/blog"><i class="fas fa-blog"></i> Tech Blog</a>
                        <a href="/terms"><i class="fas fa-file-contract"></i> Terms</a>
                        <a href="/privacy"><i class="fas fa-shield-alt"></i> Privacy</a>
                    </div>
                </div>
                <div class="footer-section">
                    <h3>Contact</h3>
                    <div class="footer-links">
                        <% if (showAdminButton) { %>
                            <a href="/admin"><i class="fas fa-user-cog"></i> Admin Panel</a>
                        <% } %>
                        <a href="#" onclick="alert('Email: support@bangaloreconnect.com')"><i class="fas fa-envelope"></i> Contact Us</a>
                        <a href="https://chat.whatsapp.com/KhTXl9CNMbSG8mv6nYCAAW" target="_blank"><i class="fab fa-whatsapp"></i> WhatsApp Group</a>
                    </div>
                </div>
            </div>
            <div class="copyright" style="font-size: 12px; padding-top: 1.5rem;">
                <span>© 2026 Bangalore Connect. All rights reserved.</span>
                <span>Built with ❤️ by GMI Tech</span>
            </div>
        </div>
    </footer>

    <script src="/script.js"></script>
</body>
</html>